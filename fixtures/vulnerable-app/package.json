{
  "name": "vulnerable-app-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "Deliberately-vulnerable HTTP/HTTPS fixture app for Security Configuration Auditor tests. Never deploy.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": { "node": ">=20" }
}
